apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pl-postgres.fullname" . }}-test-connection"
  labels:
    {{- include "pl-postgres.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pl-postgres.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
