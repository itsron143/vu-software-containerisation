apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pushups-logger-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pushups-logger-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pushups-logger-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
